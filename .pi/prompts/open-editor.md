---
description: Show Rust project configuration and validation commands
---
Use the `rust-workspace-dev` skill.

This Rust template does not define an editor launch command. Show the detected Cargo configuration with `scripts/show-config.cmd` and suggest the standard validation commands.
