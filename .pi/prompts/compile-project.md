---
description: Build the active Rust workspace or crate
---
Use the `rust-workspace-dev` skill.

Validate the Rust toolchain if needed, then build the active Rust workspace using `scripts/compile-project.cmd`. Summarize the result and any errors.
